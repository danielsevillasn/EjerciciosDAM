SQL ESTRUCTURA

SYS-CONEXIÓN

1- CREAR USUARIO ADMINISTRADOR/DUEÑO DARLE LOS PRIVILEGIOS NECESARIOS AL USUARIO DUEÑO
	CREATE USER ___ IDENTIFIED BY ___
	GRANT CREATE SESSION TO ___ WITH ADMIN OPTION
	GRANT CREATE TABLE TO ___ WITH ADMIN OPTION
	GRANT CREATE ROLE TO ___
	GRANT DROP ROLE TO ___
	GRANT CREATE USER TO ___
	GRANT DROP USER TO ___
2- CREAR LA CONEXIÓN CON EL USUARIO DUEÑO
	Nombre servicio = XEPDB1
3--SCRIPT—-
1-SCRIPT DDL-DCL
2-CREAR TABLAS Y RESTRICCIONES(CLAVES PRIMARIAS,  FORÁNEAS, CHECKS, ETC…)
	DROP TABLE ___ CASCADE CONSTRAINTS
	CONSTRAINT (NOMBRE RESTRICCION) PRIMARY KER (ATRIBUTO)
	CONSTRAINT (NOMBRE RESTRICCION) CHECK (ATRIBUTO IN (''))
	CONSTRAINT (NOMBRE RESTRICCION) FOREIGN KEY (ATRIBUTO) REFERNCES (TABLA ATRIBUTO) ON DELETE CASCADE
	UNIQUE NOT NULL a nivel de columna
	ALTER = MODIFY, ADD y DROP
3-INSERTS A LAS TABLAS

4-ADMINISTRACION DE ROLES Y USUARIOS
	-CREAR ROLES
	-ASIGNAR PRIVILEGIOS A LOS ROLES
		-CREAR USUARIOS
		-ASIGNAR ROL A USUARIO
	DROP ROLE 
	DROP USER CASCADE
	GRANT CREATE SESSION
	CREATE ROLE
	GRANT (PRIVILEGIOS = SELECT, DELETE, UPDATE y INSERT)
	CREATE USER IDENTIFIED BY
	GRANT (ROL) TO (USER)
	REVOKE (PRIVILEGIO) ON (TABLE) FROM (USUARIO O ROL)

GRANT —> ASIGNAR COSAS
REVOKE —> QUITAR COSAS